# Only non-secret config lives here. The admin password is created during
# first-run setup (on the Pi, see README) and stays inside the gitignored
# data/ dir, covered by scripts/backup.sh.
TZ=America/Chicago
