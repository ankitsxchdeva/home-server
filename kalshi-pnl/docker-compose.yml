services:
  kalshi-pnl:
    build: .
    container_name: kalshi-pnl
    restart: unless-stopped
    ports:
      - "8001:8000"          # tailscale funnel :10000 points here (public pnl number)
    volumes:
      - ./kalshi_private_key.pem:/app/kalshi_private_key.pem:ro
    env_file:
      - .env
