services:
  gform-image-embed:
    build: .
    container_name: gform-image-embed
    restart: unless-stopped
    # reap zombie chromium/node children left by per-request Playwright launches
    init: true
    env_file:
      - .env
