# Homepage Docker Configuration
# See: https://gethomepage.dev/configs/docker/
# Socket is mounted read-only in homepage/docker-compose.yml. Tiles in
# services.yaml reference it via `server: pi` + `container: <name>`.

pi:
  socket: /var/run/docker.sock
